
<div class="row">
  <div class="span9">
<table class="table table-striped">
  
<% @tutorials.each do |tutorial| %>

    <tr>

    

<!--<td><%= image_tag tutorial.image_url(:thumb) if tutorial.image? %></td>      -->

<div class="details">
<td><h3><%= link_to tutorial.title, tutorial_path(tutorial) %>

</h3>
</div>
<p class="exc"><%= tutorial.outline.html_safe %></p>
<p class="manage"><%= tutorial.steps.count %> Easy Steps | 
   Completed: <b><%= pluralize tutorial.reputation_for(:votes).to_i, "times" %></b> | Tutorial by <%= tutorial.user.username %> (Guided <b><%= tutorial.user.reputation_for(:votes).to_i %> coders </b>to safety) | <%="#{time_ago_in_words(tutorial.created_at)} ago" %>

   <!-- Tags: <% tutorial.tags.any? %>
  <% tutorial.tags.each do |tag| %>
  <%= link_to tag.name, tagged_url(:tag => tag.name) %> 
<% end %> |  Working Demo: <%= link_to tutorial.website, tutorial.website %> -->

       <% if current_user.present? && tutorial.editor?(current_user) %>
          <%= link_to "Edit", edit_tutorial_path(tutorial), :class => "btn" %>
          <%= link_to "Delete", tutorial_path(tutorial), :method => "delete", :class => "btn btn-danger" %>
        <% end %>
      </td>
    </tr>
  <% end %>
</table>
</div>
  <div class="span3">
    <div class="well info" align="center">
    <h3>About</h3>
    Community of rails developers collaboratively teaching each other to code. </div>

<!-- <div class="well" align="center">
    <h3>Makers Club</h3>
    The make club chooses one tutorial/gem per week to go through. Sign Up Today!</div>
-->

  </div>
    </div>
</div>